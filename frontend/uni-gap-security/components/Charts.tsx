'use client';import {LineChart,Line,AreaChart,Area,XAxis,YAxis,Tooltip,ResponsiveContainer,BarChart,Bar,PieChart,Pie,Cell,RadialBarChart,RadialBar} from 'recharts';import {useApp} from '@/store/app';
export function TrafficChart({kind='mbps'}:{kind?:string}){const data=useApp(s=>s.telemetry);return <div className="h-64"><ResponsiveContainer><AreaChart data={data}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--iris)" stopOpacity=".28"/><stop offset="100%" stopColor="var(--iris)" stopOpacity="0"/></linearGradient></defs><XAxis dataKey="time" hide/><YAxis hide/><Tooltip contentStyle={{background:'var(--surface)',border:'1px solid var(--border)',borderRadius:12}}/><Area type="monotone" dataKey={kind} stroke="var(--iris)" fill="url(#g)" strokeWidth={2} isAnimationActive={false}/></AreaChart></ResponsiveContainer></div>}
export function ThreatDistribution({onClick}:{onClick?:(v:string)=>void}){const a=useApp(s=>s.alerts);const data=['DDoS','Botnet C2','DGA / DNS Tunneling','Encrypted Malware','Recon / Port Scan','Data Exfiltration'].map(x=>({name:x,value:a.filter(v=>v.threat_class===x).length}));return <div className="h-64"><ResponsiveContainer><BarChart data={data} layout="vertical" onClick={(x:any)=>x?.activeLabel&&onClick?.(x.activeLabel)}><XAxis type="number" hide/><YAxis type="category" dataKey="name" width={125} tick={{fontSize:10,fill:'var(--muted)'}}/><Tooltip/><Bar dataKey="value" fill="var(--iris)" radius={[0,8,8,0]}/></BarChart></ResponsiveContainer></div>}
export function SeverityChart(){const a=useApp(s=>s.alerts);const data=['Critical','High','Medium','Low'].map(x=>({name:x,value:a.filter(v=>v.severity===x).length}));return <div className="h-56"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={58} outerRadius={82} paddingAngle={3}>{data.map((_,i)=><Cell key={i} fill={['var(--coral)','var(--apricot)','var(--gold)','var(--mint)'][i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div>}
export function Confidence(){const avg=useApp(s=>s.alerts.reduce((x,a)=>x+a.confidence,0)/Math.max(1,s.alerts.length));return <div className="h-44"><ResponsiveContainer><RadialBarChart innerRadius="65%" outerRadius="95%" data={[{value:avg*100,fill:'var(--iris)'}]} startAngle={90} endAngle={-270}><RadialBar dataKey="value" cornerRadius={10} background/><text x="50%" y="50%" textAnchor="middle" dominantBaseline="middle" className="fill-current font-bold text-2xl">{(avg*100).toFixed(1)}%</text></RadialBarChart></ResponsiveContainer></div>}
export function Spark({dataKey='mbps'}:{dataKey?:string}){const data=useApp(s=>s.telemetry.slice(-24));return <div className="h-16"><ResponsiveContainer><LineChart data={data}><Line dataKey={dataKey} stroke="var(--mint)" dot={false} strokeWidth={2} isAnimationActive={false}/></LineChart></ResponsiveContainer></div>}
